const tpl = `
      <section class="form_wrapper">
        <form class="form_body">
          <h3 class="form_title">{{title}}</h3>
          {{{fields}}}
          <div class="form_buttons">
          {{{buttons}}}
          </div>
        </form>
      </section>`;

export default tpl;
